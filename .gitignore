__pycache__/
*.pyc
.ipynb_checkpoints/

# raw audio + downloaded archives (too large for git)
data/raw/
data/*.tar.gz
data/*.zip
data/*.flac
data/*.wav

# cached extracted features (regenerable, can be large)
data/features/
*.npz

# experiment outputs (regenerated by src/run_experiment.py) and trained models
results/
*.joblib

# local env
.venv/
venv/
